# Marker file so pytest treats `integrations.agentfield` as an importable
# package. No runtime content. The additive adapter lives under this tree;
# nothing else in the codebase imports from this package.
