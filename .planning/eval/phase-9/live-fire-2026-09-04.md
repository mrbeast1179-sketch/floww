# Live-Fire Results 2026-09-04 — Agent 4 ( Task 2.1 + 2.2 )

**Method:** GET /api/public/chain/{SPY,QQQ} (fresh, stale=false) → harness/live_fire.py →
IMPORTED scan_score/est_entry/biz_dte (no reimplementation). Backend :8000 healthy at run time.

## Measured fire rates (live, real code path)

| Gate | SPY (1159 vol rows) | QQQ (1119 vol rows) |
|---|---|---|
| SCORE≥92 | 26 (2.24%) | 40 (3.58%) |
| WHALE ≥$25M | 51 (4.40%) | 57 (5.09%) |
| 0DTE volOI≥2 | n/a (0 rows with dte==0; same-day expiry counts ≥1 in biz_dte) | n/a |
| SIGMA≥6σ | UNMEASURABLE (no B1 baselines) | UNMEASURABLE |
| score mean / max | 56.4 / 100 | (dist: ≥80: SPY 160, tail to 100) |

Top SPY rows: 780C/766P 09-04 scoring 98–100, volOI 3–8, premium $120–300M (BS est_entry).

## Measured-vs-estimated deltas (alert-gate-economics.md is revised accordingly)

1. SCORE≥92: estimated 5–10% pass; measured 2.2–3.6% → divergence >25% → REVISED to 96–98%
   suppressed. Direction: gate is TIGHTER than estimated (good for noise, watch for starvation).
2. WHALE: estimated "~99%+ suppressed"; measured ~95% suppressed (4–5% fire on index ETFs) →
   LARGE divergence → REVISED. Cause: premium via BS est_entry on mega-cap volume inflates notional;
   $25M is not rare on SPY/QQQ. Recommend: tier WHALE by underlying liquidity class, or gate on
   notional=vol×strike×100 alongside modeled premium.
3. SIGMA/0DTE/TTL/cap: fixture-only until B1; doc marked accordingly (no estimates presented as measured).
4. Dedup + 4/hour: not observable in a single snapshot; need multi-poll observation (deferred).

## Outcomes pipeline: BROKEN (live evidence)

GET /api/flowseeker/alerts/quality?days=30 → 25 measured alerts, hit_rate 0.0 and avg_move_pct
EXACTLY 0.0 on every rule × tier. update_moves stamps move=(last−under_price)/under_price on the scan
path (flowseeker.py:754 → flow_alerts.py:950), so exact-zero means stamps land same-snapshot or the
:memory: DuckDB ledger evaporates on backend restart (known crash-prone) before moves accumulate.
Either way the calibration loop currently measures NOTHING. This is live evidence for B1 file-backed
DuckDB (retention ≥30d) as P0: without it, conviction_calibration + alert_quality are decoration.
Filed as F20 (P0): outcome ledger non-persistent; quality endpoint serves 0.0s.

## Task 2.2 score boundaries via actual code

scan_score executed live (see dist above: full 0–100 range exercised, boundaries 92/95 populated).
Frontend rowConviction/pulseScore10 (JSX, not node-importable): verified via shipped Jest suite
(W2 commit message: flowseeker trio 156/156 green) — cited, not re-run from this lane. Signed-overlay
spec boundaries (S_MAX/S_MIN/S_ZERO) remain fixture-level: overlay is not implemented (CR-10 open).

## Task 2.3 backtest harness

backtest_harness.py DOES NOT EXIST (searched backend/, .planning). Existing engine
(backend/services/backtest/engine.py) is trade-based (P/L, drawdown) with RuleBasedSignal/
MLEnrichedSignal — no SCORE≥92 precision/recall concept. The precision path that DOES exist is
alert_quality (per rule × tier, live data above) — currently 0.0s per F20. No harness fix needed;
the missing piece is outcome persistence (F20), not harness code. Blocker command recorded:
`curl -s 'http://localhost:8000/api/flowseeker/alerts/quality?days=30'` → all hit_rate 0.0.
