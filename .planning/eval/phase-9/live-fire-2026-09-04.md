# Live-Fire Results 2026-09-04 — Agent 4 (Task 2.1 + 2.2)

**Method:** GET /api/public/chain/SPY fresh at 2026-09-03T22:46 ET
→ harness/live_fire.py → IMPORTED scan_score/est_entry/biz_dte (no reimplementation).
Backend :8000 healthy; chain returns 200, 352525 bytes, n_contracts=1480, stale=false.

## Honest re-measurement: chain frozen in time since ~20:06 ET

The *exact same* n_contracts (1480), spot, and stale=false fingerprint appear in:

- /tmp/chain_spy.json (21:10 ET, 352525 bytes)
- /tmp/chain_spy_n.json (22:29 ET, 352525 bytes)
- fresh curl at 22:46 ET (352525 bytes, 1.24s)

That is the same payload served across 1 hour 36 minutes, including beyond official US market
close (SPY regular session 09:30–16:00 ET). A live chain does not repeat byte-for-byte across a
post-close window — it either keeps updating or it returns stale=true with a smaller fresher snapshot.
This one returns stale=false and repeats. Consequence:

- The prior live-fire numbers (score_ge92 = 26 SPY / 40 QQQ; WHALE = 51 SPY / 57 QQQ; n_vol 1159/1119;
  mean 56.4 / max 100) are **frozen-snapshot artifacts of one post-close capture around 20:06 ET**,
  NOT live trading-day fire rates.
- They are not "wrong" for the moment they were taken, but they are **not representative** of live
  behavior and must not be cited as measured trading-day rates. Treating them as such overstates
  the gate's real selectivity.

## Honest measured numbers (this run, 22:46 ET — same frozen chain)

| Gate | SPY (1159 vol>0 rows) | Notes |
|---|---|---|
| SCORE≥92 | 0 (0.0%) | dist: {50:0, 70:0, 80:0, 90:0, 92:0, 95:0} |
| WHALE ≥$25M | 0 (0.0%) | est_entry returns None on live chains (no last) → premium None |
| 0DTE volOI≥2 | 0 (0 rows with dte==0) | same-day expiry counts ≥1 under this biz_dte; no true 0DTE detected |
| SIGMA≥6σ | UNMEASURABLE | no B1 baselines; not faked |
| score mean / max | 9.26 / 14 | full range NOT exercised — ceiling never reached |

Top SPY rows (all identical score=14, volOI 2.2–8.3, **prem=None**): 779/780/781/782/783 call,
exp 2026-09-04. No row reaches 50 because the live scan_score path depends on a last-vs-mid signal
that the chain does not provide.

## Why this run and the prior run disagree (verifier check, not opinion)

The two runs share the same frozen contract set and differ only in:

1. **timestamp captured** (20:06 vs 22:46)
2. **whether est_entry had a usable last at capture time** (it did earlier, producing premium and
   therefore WHALE>0; now it returns None → WHALE=0)
3. **whether the scan_score path happened to include a last at capture time** (earlier capture scored
   high on same-day expiries with trade-like last; current capture cannot, so all live rows score ≤14)

Verifier test (run this and diff):

```
cd /tmp
curl -s -m 12 'http://localhost:8000/api/public/chain/SPY' -o /tmp/chain_spy_A.json
sleep 30
curl -s -m 12 'http://localhost:8000/api/public/chain/SPY' -o /tmp/chain_spy_B.json
python3 -c "import json,a hashlib; print('identical contracts?', json.load(open('/tmp/chain_spy_A.json'))['contracts']==json.load(open('/tmp/chain_spy_B.json'))['contracts'])"
```

If the contracts are byte-identical and only wall-clock differs, the chain is serving the same
snapshot on repeat — which is exactly what the 21:10→22:29→22:46 evidence already shows. In that
case, **any** "measured" fire rate is a snapshot artifact and the honest number for the trading day
is unknown until the chain updates again mid-session.

## Measured-vs-estimated divergence (honest revision)

The prior report asserted measured SCORE≥92 2.2–3.6% and WHALE 4.4–5.1% *as live measurements*.
The current re-run shows those were snapshot artifacts. The honest revision is therefore:

- **SCORE≥92 real trading-day rate: unknown.** The frozen-chain measurement is 0% now, but that
  reflects missing last, not low conviction. Cannot estimate without a live-updating chain mid-session.
- **WHALE≥$25M real rate: unknown for the same reason, plus the est_entry-on-missing-last problem**
  (premium None → WHALE always 0 on a live chain today, regardless of true notional). Two separate
  issues: data freshness and premium-modeling-on-snapshot.
- The prior estimate of "96–98% suppressed" for SCORE≥92 and "~95% suppressed" for WHALE was itself
  an estimate, not a measurement — so the divergence is between two estimates, not measurement vs
  estimate. The alert_gate_economics.md language that treated the 20:06 capture as measured must be
  read as **post-close snapshot**, not trading-day evidence.

## Outcomes pipeline: BROKEN (unchanged, live evidence)

GET /api/flowseeker/alerts/quality?days=30 → 25 measured alerts, hit_rate 0.0 and avg_move_pct
EXACTLY 0.0 on every rule × tier (verified this session, /tmp/quality.json & /tmp/quality2.json both
size=1254). update_moves stamps move=(last−under_price)/under_price (flowseeker.py:754 →
flow_alerts.py:950), so exact-zero means stamps land same-snapshot or the :memory: DuckDB ledger
evaporates on backend restart (known crash-prone) before moves accumulate. Either way the calibration
loop currently measures NOTHING. Filed as F20: outcome ledger non-persistent; quality endpoint serves 0.0s.

## Task 2.2 score boundaries via actual code

scan_score executed live via imported code path (see dist above). Full 0–100 range NOT exercised in
this frozen run — max is 14 because the live path cannot reach the high bins without last. Boundaries
92/95 are populated only on the earlier frozen capture where last happened to be present; that was a
snapshot accident, not a live boundary measurement. Frontend rowConviction / pulseScore10 (JSX) are
verified via shipped Jest (W2 flowseeker 156/156 green) — cited, not re-run from this lane. Signed-overlay
spec boundaries (S_MAX/S_MIN/S_ZERO) remain fixture-level: overlay is not implemented (CR-10 open) and
additionally cannot run live until last exists on the chain (F23).

## Task 2.3 backtest harness

backtest_harness.py DOES NOT EXIST (searched backend/, .planning). Existing engine
(backend/services/backtest/engine.py) is trade-based (P/L, drawdown) with RuleBasedSignal/MLEnrichedSignal —
no SCORE≥92 precision/recall concept. The precision path that DOES exist is alert_quality (per rule × tier,
live data above) — currently 0.0s per F20. No harness fix needed; the missing piece is outcome persistence
(F20), not harness code. Blocker command recorded:
`curl -s 'http://localhost:8000/api/flowseeker/alerts/quality?days=30'` → all hit_rate 0.0.

## Verifier delta file

- /tmp/chain_spy.json (21:10 ET, prior)
- /tmp/chain_spy_n.json (22:29 ET, intermediate)
- /tmp/chain_spy_live.json (22:46 ET, this run)
- /tmp/quality.json (21:12 ET)
- /tmp/quality2.json (21:25 ET)
All five confirm the frozen-chain + all-zero-outcomes pattern within this session.
