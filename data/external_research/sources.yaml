# sources.yaml — curated research-discovery queries
#
# Schema:
#   <source_name>:
#     queries:           # for keyword-based sources (arxiv, HF)
#       - "string"
#     topics:            # for GitHub-topic-based discovery
#       - "topic-slug"
#
# Source names must match `DiscoverySource.name` attributes in
# backend/services/research/discovery.py.
#
# Run: `python scripts/discover_research.py` (when wired) reads this file and
# searches each source. Findings are written to discoveries_<UTC-date>.json.

arxiv:
  queries:
    # Core market-microstructure / options topics — peer-reviewed quant
    - "gamma exposure dealer hedging"
    - "options market making inventory"
    - "implied volatility surface dynamics"
    - "0DTE options dealer positioning"
    - "vanna charm exposure dealer"
    - "options flow informed trading"
    - "machine learning options price prediction"
    - "ETF options demand pressure"
    # Risk / regime
    - "market regime detection volatility"
    - "VIX term structure forecasting"
    # New topics — Autonomous Research Track (2026-07-09)
    - "options market microstructure"
    - "dealer gamma exposure"
    - "Hawkes process trade arrivals"
    - "limit order book transformer"
    - "1D-CNN financial anomaly"
    - "VPIN flow toxicity"
    - "SABR model calibration"
    - "SVI volatility surface"
    - "stochastic volatility jump diffusion"
    - "order flow toxicity machine learning"
    - "market making reinforcement learning"
    - "high frequency trading limit order book"
    - "neural network option pricing"
    - "volatility surface deep learning"
    - "cross asset volatility spillover"

huggingface:
  queries:
    - "options trading"
    - "dealer gamma"
    - "1D CNN autoencoder financial"
    - "limit order book"
    - "PatchTST options"
    - "financial time series"
    - "volatility forecasting"
    - "market microstructure"
    - "options flow"
    - "gamma exposure"

github_topic:
  topics:
    - options-trading
    - quantitative-finance
    - gamma-exposure
    - options-flow
    - market-microstructure
    - implied-volatility
    - black-scholes
    - limit-order-book
    - hawkes-process
    - volatility-surface
    - options-pricing
    - algorithmic-trading

# ────────────────────────────────────────────────────────────────────────────
# Already-cloned reference repos
# ────────────────────────────────────────────────────────────────────────────
# These live under data/github-repos/cloned/ — kept here for inventory.
# Run `ls data/github-repos/cloned/` for the current set.

reference_repos:
  - name: iAmGiG_gex-llm-patterns
    purpose: "academic GEX dataset (243 rows) + statistical validation"
    license: "check repo LICENSE"
  - name: aaguiar10_gflows
    purpose: "CBOE quote data CSV (SPX/NDX/RUT)"
    license: "check repo LICENSE"
  - name: FullStackCraft_floe
    purpose: "TypeScript options analytics — reference for VEX/DEX/PDF math"
    license: "check repo LICENSE"
  - name: FlashAlpha-lab_gex-explained
    purpose: "vendor sample chain (26 rows) — fixture for unit tests"
    license: "check repo LICENSE"
  - name: EsterHlav_Black-Scholes-Option-Pricing-Model
    purpose: "BS reference for canonical Greeks tests"
    license: "check repo LICENSE"
  - name: MattL922_implied-volatility
    purpose: "IV solver reference"
    license: "check repo LICENSE"
  - name: Matteo-Ferrara_gex-tracker
    purpose: "CBOE-scraping GEX calculator — cross-check"
    license: "check repo LICENSE"
