# floww — Feature Integration Roadmap ("Steal List")

_Generated 2026-07-11 by a multi-agent scan of floww's 26 cloned reference repos + web discovery, synthesized by Fable 5._

_Restored 2026-07-15: a status edit clobbered the doc header + items #1–#8; rebuilt from git HEAD with statuses merged back in. Every ✅ below was re-verified against bytes and live test runs on 2026-07-15 (282 backend tests green across the steal-list suites), not taken from session claims._

**Scope:** capabilities floww does NOT yet have, portable onto its free/cvforge + yfinance data (no paid tick feeds, no broker execution). Ranked by value/effort. Effort 1=hours, 5=weeks. Value 1-9.

## Themes

- **Buy the realized side of the trade** — Floww is entirely implied/positioning-side today. The single biggest value cluster (RV suite + VRP, typical ranges, earnings screener, wheel screener) is about benchmarking implied against realized — is this premium rich? — which is precisely the premium-selling edge floww's journal has already validated. Four of the sixteen items chain together into one 'is vol rich or cheap' answer.
- **From read to trade: close the last mile** — Floww tells you what dealers are doing but not what YOU should structure. The strategy builder (PoP/payoff), strike cone, wheel screener, scenario stress grid, and eventually the backtester convert existing analytics into concrete strikes, structures, odds, and book-level risk — the layer where a retail user actually acts.
- **Fix the inputs before adding more outputs** — Everything downstream — GEX, flip, King node, RND, PoP — inherits yfinance's noisy last-price IVs and stale 0DTE OI. Three cheap items (IV-from-mid solver, 0DTE volume-for-OI, and later the SVI arb-free surface) form a staged input-quality pipeline that quietly improves every existing panel with zero new UI.
- **Positioning as a movie, not a snapshot** — Current GEX is a point-in-time, current-spot, OI-only picture. The profile engine (exposure vs hypothetical spot), dual-GEX activity ratio (flow vs stock), regime-persistence classifier (30-day stability), max-pain drift, and OCC account-type data each add a missing dimension: price, activity, time, and actor.
- **Sequencing matters: three dependency chains** — Ship in dependency order: (a) IV solver → RND → SVI surface; (b) RV service → VRP tile → earnings screener; (c) strategy payoff engine → scenario grid → backtester. The quick wins (ranks 1-3, 5, 8-10) are order-independent and can land in any single session.

## Roadmap — vol / GEX / pricing / strategy

### ~~#1 · Dual-GEX: OI vs volume-weighted gamma + hedging-intensity activity ratio  `[quick-win]`  — value 6 / effort 1~~
- **Status:** ✅ **DONE** (2026-07-14, steal-three session).
  - **In:** `backend/services/gex_dual.py` (`DualGexCalculator`) + `GET /api/dual_gex/{ticker}` in the consolidated steal-three router (canonical :8000 via `server.py`, dev sidecar :8001 via `services/steal_three_server.py`). Frontend `DualGEXBadge.jsx` mounted on Heatseeker Row 3 and the Skylit steal-list band. Suite green at `backend/tests/services/test_gex_dual.py`.
- **Why:** Highest value/effort on the board (6:1). One extra aggregation pass distinguishes what dealers structurally HOLD from what they are actively DOING today — identical flip levels behave differently when flow fights vs confirms resting OI, which floww's OI-only GEX cannot see.
- **Steal from:** `iAmGiG_gex-llm-patterns/src/gex/gex_calculator.py:271-366 (calculate_dual_gex)`
- **Lands in floww:** Extend backend/services/gex_aggregator.py compute() to also aggregate GEX weighted by contract volume (already in chain dicts via routes/heatseeker.py:43); emit gex_volume + activity_ratio = |GEX_vol/GEX_OI| as a 'hedging intensity' badge on the dealer-positioning panel.

### ~~#2 · 0DTE/1DTE exposure weighted by same-day volume instead of stale OI  `[quick-win]`  — value 5 / effort 1~~
- **Status:** ✅ **DONE** (2026-07-15, env-gated).
  - **In:** Short-DTE (T ≤ 1/252) volume-for-OI substitution with fillna-to-OI in `backend/services/gex_aggregator.py`, gated behind `FLOWW_USE_VOL_FOR_SHORT_DTE` (default **OFF** so the S² display oracle and the model-locked S¹ feature path are untouched — flipping it on for display is a product decision, not a code change). Suite green at `backend/tests/services/test_gex_aggregator_short_dte.py`; S²/S¹ golden oracle still green at `test_gex_aggregator_oracle.py`.
- **Why:** 0DTE dominates SPX/SPY/QQQ dealer gamma and day-of OI hasn't printed yet, so floww systematically mis-weights the bucket that drives most intraday hedging. A one-branch data-quality fix that improves every downstream gamma signal (heatmap, flip, King node).
- **Steal from:** `aaguiar10_gflows/modules/calc.py:339-364 (is_short_dte volume-for-OI substitution, fillna to OI)`
- **Lands in floww:** In backend/services/gex_aggregator.py and the gex_history compute path, branch on T <= 1/252 and select volume (fillna OI) as the per-contract weight. Volume already flows through heatseeker.py:43.

### ~~#3 · Wheel / premium-selling income screener (CSP + covered calls ranked by annualized return on collateral)  `[quick-win]`  — value 7 / effort 2~~
- **Status:** ✅ **DONE** (2026-07-14, steal-three session).
  - **In:** `backend/services/screeners/wheel_income.py` (`rank_puts_to_sell` / `rank_calls_to_sell`, ARR + breakeven + IV/volume/DTE filters) + `GET /api/screener/income?symbol=&side=put|call|both` in the steal-three router. Frontend `WheelIncomeScreenerPanel.jsx` (CSP/CC tabs, pill filters, sortable table) on Heatseeker Row 3 + Skylit band. Suite green at `backend/tests/services/screeners/test_wheel_income.py`.
- **Why:** Directly serves floww's only validated edge — premium selling. Nothing in floww ranks contracts by income yield or breakeven; Flowseeker ranks by flow archetype, which is orthogonal. Cross-tagging with IV-rank floats rich-vol, high-yield names to the top.
- **Steal from:** `fanzhenya_options_lab/options_lab.ipynb — find_best_put_to_sell, find_best_call_to_sell, annualize_return/calc_put_breakeven`
- **Lands in floww:** New backend/services/screeners/wheel_income.py iterating yfinance option_chain over expirations, computing ARR + breakeven + IV/volume filters; expose /api/screener/income?symbol=&side=put|call&max_breakeven=; sortable table in the existing scanner UI.

### ~~#4 · Risk-neutral density (Breeden-Litzenberger implied PDF) from the chain  `[high-impact]`  — value 9 / effort 3~~
- **Status:** ✅ **DONE** (2026-07-15, this session — risk-neutral-density).
  - **In:** Pure-logic `backend/services/risk_neutral_density.py` porting the Breeden-Litzenberger formula `f_Q(K) = e^{r·T} · d²C(K)/dK²`. Cubic-spline smoothing of the per-strike call surface via `scipy.interpolate.CubicSpline` with central-difference numerical 2nd derivative as fallback for n<4 strikes or scipy-unavailable environments (defensive degrade signature parity with strike_cone's NaN-safe defensive guards). Strike-pruning window [0.7·spot, 1.3·spot] filters far-OTM/ITM tail noise. 200-point evaluation grid spanning the pruned strike range. Output: PDF (clipped ≥ 0 + renormalised via trapezoid-rule so integral ≈ 1.0), CDF (cumulative trapezoid + forced to 1.0 at K_max), expected_price (∫ K · f_Q(K) dK), expected_move_pct, median (CDF crosses 0.5), mode (argmax of pdf), and tail_probs dict for 95 / 98 % spot-relative put-side and 102 / 105 % call-side thresholds (returns `None` when the threshold falls outside the grid range — keeps the dashboard rendering crash-free). Endpoint `GET /api/rnd/{ticker}?expiry_index=N` mounted in canonical steal-three router at :8000 (default expiry_index=1 skips 0DTE because T=0 makes the BL 2nd derivative degenerate — same guard as strike_cone's `min_dte=1`). Endpoint layer reuses `_load_multi_expiry_chain` + filters to calls-only + computes T from expiry ISO date. 20-case hand-verified unit suite ships green at `backend/tests/services/test_risk_neutral_density.py` (empty/single/three/five/symmetric/OTM-only/IV-width/integration/monotonicity/CDF-end/expected_price/median/mode/tail-prob-sanity/r-zero/T-zero/negative-strikes/NaN-inf/schema-contract).
- **Why:** Adds a whole missing axis: price-space probability. 'Market prices a 12% chance of SPY < 580 by Friday' is the single most legible output for a retail trader and nothing in GEX/Trinity expresses it. Cheap because floww already has the chain, scipy smoothing, and BS repricing. Natural consumer of the clean-IV work (rank 5) and feeder for the strike cone (rank 10).
- **Steal from:** `PavanAnanthSharma/Breeden-Litzenberger-formula-for-risk-neutral-densities (smile fit -> reprice calls -> d2C/dK2 = e^{rT} f_Q(K))`
- **Lands in floww:** Pure-logic `backend/services/risk_neutral_density.py` (sits at `backend/services/` per the established steal-list convention rather than the legacy `backend/analytics/` reference in the doc); cubic-spline smile smoothing + numerical 2nd derivative; `GET /api/rnd/{ticker}?expiry_index=N`; React PDF/CDF panel beside the Trinity smiles with tail-prob and expected-move readouts (not yet mounted — frontend pass is the followup).

### ~~#5 · IV inversion solver — recompute clean IV from bid/ask mids  `[quick-win]`  — value 6 / effort 2~~
- **Status:** ✅ **DONE** (2026-07-14, steal-three session).
  - **In:** `implied_vol_from_price()` in `backend/bs_greeks.py` (Newton via bs_vega with bisection fallback) + `GET /api/iv_mid/{ticker}?width=N` cross-check endpoint in the steal-three router. Frontend `IVMidBadge.jsx` (yfinance IV vs solved-from-mid per ATM-ish strike + round-trip residual) on Heatseeker Row 3 + Skylit band. Suite green at `backend/tests/services/test_implied_vol_solver.py`. The 'clean IV' toggle into `vol_analytics.calc_iv_surface_data` is still open (endpoint proves the solver; surface wiring is a follow-up).
- **Why:** Floww's entire vol stack trusts yfinance's impliedVolatility field, which is stale, last-price-based, and missing on illiquid strikes. ~15 lines (Newton via existing bs_vega, bisection fallback) cleans the input to skew, term structure, RR, GEX, and the RND above. Also stage 1 of the SVI surface (rank 16).
- **Steal from:** `MattL922_implied-volatility/implied-volatility.js::getImpliedVolatility (upgrade bisection to Newton using floww's bs_vega)`
- **Lands in floww:** Add implied_vol_from_price() to backend/bs_greeks.py; wire into vol_analytics.calc_iv_surface_data preferring solver(mid) over raw yfinance IV, with a 'clean IV' toggle; q from yfinance dividendYield.

### #6 · Spot-shifted exposure PROFILE engine (gamma/vanna/charm/delta vs price curves + true flip points)  `[high-impact]`  — value 8 / effort 3
- **Status:** ⬜ not started.
- **Why:** Upgrades floww's flip from 'strike where per-strike GEX crosses zero' to the SqueezeMetrics-style true gamma-neutral and delta-neutral spot, interpolated from aggregate exposure recomputed across 300 hypothetical spot levels. Also yields vanna/charm PROFILE curves (vanna wall, charm-into-close) and 0DTE-vs-structural decomposition via per-expiry masks. Highly portable — floww's vectorized numba greeks already exist; only the spot-grid loop and flip solver are new.
- **Steal from:** `aaguiar10_gflows/modules/calc.py:306-749 (calc_exposures, zerodelta/zerogamma) + modules/stats.py:44-121`
- **Lands in floww:** New ExposureProfileService looping backend/services/numba_greeks.py over np.linspace(0.5*spot, 1.5*spot, 300) using existing databento_eod_chains data; new route beside routes/greeks.py; 'Exposure Profile' curve chart on Skylit next to the GEX heatmap, complementing zero_gamma_levels.

### #7 · Realized-vol suite + variance risk premium (Yang-Zhang, GK, Parkinson, vol cones) with realized-range percentile bands  `[high-impact]`  — value 8 / effort 3
- **Status:** ⬜ not started. (Prerequisite for #13; feeds the wheel screener gate.)
- **Why:** Trinity is entirely IV-side, so floww literally cannot compute IV-minus-RV — the core premium-seller edge — or judge if today's implied move is rich vs how far the name actually travels. Merges two candidates: volest's estimator suite + cones, and EzOptions' daily/weekly/monthly typical-range p50/p80/p95 bands overlaid with floww's existing calc_implied_move.
- **Steal from:** `jasonstrimpel/volatility-trading volest.VolatilityEstimator (cones, rolling_quantiles) + EazyDuz1t_EzOptions/ezoptions.py calculate_typical_ranges (L1018)`
- **Lands in floww:** New RV service fed by yfinance OHLC floww already pulls; /vol/realized returning 20/30/60d RV across estimators + cone percentile + range bands; VRP tile (front ATM IV − Yang-Zhang RV) beside IV/GEX panels; gate premium-selling ideas (wheel screener, rank 3) on it. Prerequisite for the earnings screener (rank 13).

### ~~#8 · 30-day net-GEX regime-persistence classifier  `[quick-win]`  — value 6 / effort 2~~
- **Status:** ✅ **DONE** (2026-07-15, this session — regime-persistence classifier).
  - **In:** Pure-logic `classify_window(rows, window_label="30d")` in `backend/services/regime_persistence.py` — returns `{regime, sign_persistence_pct, flip_count, magnitude_conviction, coefficient_of_variation, n_days_covered, window_label, warnings}` with regime ∈ {persistent_positive, persistent_negative, transitional, low_conviction, None}. Threshold constants exported via `__all__`: `PERSISTENCE_THRESHOLD=0.75` (sign-persistence majority threshold), `CV_LOW_CONVICTION_MAX=0.10` (low-spread regime gate), `TRANSITIONAL_FLIP_DENOM=4` (≥ n/4 day-boundary flips), `NOISE_FLOOR_ABS=1.0` (absolute-magnitude gate that catches near-zero windows before flip-count noise). Arbitration precedence (read carefully — each branch is a fingerprint for a distinct regime shape): persistent (≥75% majority sign) → low_conviction (window below 1.0 noise floor — flips aren't economically meaningful) → transitional (≥25% flips) → low_conviction (small CV) → inconclusive (None). NaN/inf/missing-key rows silently dropped with warnings; never crashes on malformed data. Sync wrapper `get_gex_history_sync(ticker, days, mongo_db)` added to `backend/services/gex_history.py` to bridge the sync route (`def regime_persistence_endpoint`) to the existing async `build_gex_history` via `asyncio.run` with a fallback `new_event_loop` for threadpool contexts. Endpoint `GET /api/regime_persistence/{ticker}?days=30` mounted in canonical steal-three router at :8000 (mirrors consensus_drift + max_pain_drift pattern; uses `from server import db as mongo_db` per the precedent at `routes/analytics.py:336`, defensive try/except returns `regime=None` + empty metrics on MongoDB failure). 13-case unit suite ships green at `backend/tests/services/test_regime_persistence.py` (empty-rows + single-snapshot + 30-day-all-positive + 30-day-all-negative + alternating-signs-transitional + near-zero-noise-low-conviction + stable-7-day + NaN/inf-filter + missing-key-filter + window-label-propagation + documented-keys + 50/50-mix-boundary + module-constants-sanity).
- **Why:** Floww classifies each day long/short gamma but never says whether the regime is a durable tradable backdrop or transitional chop. Pure post-processing (sign-persistence %, flip count, magnitude conviction, CV) over a time series floww already builds.
- **Why:** Floww classifies each day long/short gamma but never says whether the regime is a durable tradable backdrop or transitional chop. Pure post-processing (sign-persistence %, flip count, magnitude conviction, CV) over a time series floww already builds.
- **Steal from:** `iAmGiG_gex-llm-patterns/src/validation/regime_classifier.py:110-243 (classify_window, _classify_regime_type)`
- **Lands in floww:** Feed backend/services/gex_history.py:build_gex_history per-day gex_total into a ported RegimeClassifier; regime badge (persistent_positive / persistent_negative / low_conviction / transitional) on the Skylit dealer-positioning panel.

### ~~#9 · Max pain + max-pain drift tracking into expiry  `[quick-win]`  — value 6 / effort 2~~
- **Status:** ✅ **DONE (FULL)** (2026-07-15, this session — full completion: max-pain algorithm + endpoint 2026-07-14; drift stats 2026-07-15 PARTIAL; per-expiry schema + scheduler cron + 30-day default this turn).
  - **In:** Pure-logic `compute_max_pain_drift(snapshots)` in `backend/services/max_pain_drift.py` — returns drift_strike_1d, drift_strike_Nd, drift_toward_now (= |oldest - spot| - |today - spot|; positive = pin migrating toward spot), drift_velocity_per_day, pin_probability_score (in [0,1] decay from 1.0 at strike==spot to 0.0 at |strike - spot|>=5% spot), direction_label (migrating_toward_spot/migrating_away/stable/insufficient_data), n_days_covered, warnings. DuckDB I/O helpers: `init_max_pain_daily_table(engine)` (idempotent CREATE TABLE IF NOT EXISTS + schema migration detecting legacy PK (snapshot_date, symbol) and DROP+recreating with new PRIMARY KEY (snapshot_date, symbol, expiry) + 2 indexes), `accumulate_today(engine, ticker, spot, max_pain_row, snapshot_date=None, expiry="")` (ON CONFLICT (snapshot_date, symbol, expiry) idempotent UPSERT — same-day re-runs overwrite row cleanly), `accumulate_today_per_expiry(engine, ticker, spot, per_expiry_rows, snapshot_date=None)` (batched single-execute_write write of one row per listed expiry — enables per-expiry pin-tracking separate from the overall pin row), `read_recent_drift(engine, ticker, n_days)` (DESC LIMIT N then reverse to ASC for natural drift walks). Endpoint `GET /api/max_pain_drift/{ticker}?days=30&accumulate=true|false` mounted in canonical steal-three router at :8000 (default bumped from 14→30 days; `accumulate=true` writes both the overall pin row AND one row per listed expiry in a single chained call). Scheduler: `_poll_max_pain_for_universe` in `backend/services/scheduler.py` runs daily (every 1440 ticks at 60s interval = 24h) over the top-10 universe (SPY/QQQ/AAPL/TSLA/NVDA/AMZN/MSFT/META/GOOGL/AMD), staggered 5 ticks from `_poll_news_for_universe` to avoid same-tick collision. 22-case unit suite ships green (18 pre-existing + 4 new for PARTIAL → DONE: per-expiry-writes-N-rows-and-UPSERT-updates/schema-migration-drops-legacy-then-recreates/scheduler-has-poll-method/route-default-days-30) at `backend/tests/services/test_max_pain_drift.py` (empty/single/2-snap/7-day stable/migrating-toward/migrating-away/long-pin-converged/auto-sort/NaN-spot/missing-strike/future-dated/documented-keys + init-idempotent/accumulate-UPSERT/three-ticker-isolated/read-recent-N/empty-table/DB-exception-suppressed + per-expiry-writes-N-rows-and-UPSERT-updates/schema-migration-drops-legacy-then-recreates/scheduler-has-poll-method/route-default-days-30). Frontend: `MaxPainDriftTile` in `ConfluenceVelocityRow.jsx` on Heatseeker Row 3.
- **Why:** OI-weighted pinning magnet genuinely distinct from the King node (gamma-based); the two often disagree, and the daily drift of max pain toward spot into expiry is a testable pin signal for 0DTE/weekly names. Zero new data — computed from OI already fetched for GEX.
- **Steal from:** `asad70/Options-Max-Pain-Calculator OptionsMaxPainCalc.py + ZubZubZuberi/MaxPainHistory`
- **Lands in floww:** Pure `backend/services/max_pain.py` exports `compute_max_pain_per_expiry(chain)` + `compute_overall_max_pain(chain)`. Lower-strike tie-break; defensive against missing `openInterest` / `expiry` / `strike` keys + fractional strikes + zero-OI chains; OI clamped to non-negative. Route `GET /api/max_pain/{ticker}?expiries=N` added to `backend/routes/steal_three.py` (`_load_multi_expiry_chain` helper caps the network blast at the first N listed expiries).

### ~~#10 · 16-delta/30-delta expected-range strike cone  `[quick-win]`  — value 5 / effort 2~~
- **Status:** ✅ **DONE** (2026-07-15, this session — strike-cone).
  - **In:** Pure-logic `backend/services/strike_cone.py::compute_cone(prob_distribution, spot, target_probs, target_deltas)` — takes the existing `calc_probability_distribution(spot, contracts)` output (per-strike {prob_above, prob_below, delta, iv}) and runs bisect+linear-interp on a monotonicity-guarded, NaN-safe value array to emit both `prob_cones` and `delta_cones` with strike_above / strike_below pairs + per-cone warnings. `GET /api/strike_cone/{ticker}?target_probs=0.16,0.30,0.70,0.84&target_deltas=0.16,0.30&expiries=1` mounted in the canonical steal-three router at :8000 (uses `services.strike_cone.compute_cone` → `server.calc_probability_distribution` → `server.fetch_spot_and_chains` chain). 17-case unit suite ships green at `backend/tests/services/test_strike_cone.py` (empty/single/two/three/exact-match/OOB-top/OOB-bottom/reversed/jittery/large-target-delta/negative-strikes/detached-spot/missing-keys/NaN/0.50-edge/key-set contract / no-targets). Frontend: `StrikeConeTile` in `ConfluenceVelocityRow.jsx`.
- **Why:** Converts floww's existing prob_above distribution into the concrete output premium sellers act on: the actual strikes at P=16/84% and 30/70%. Small and incremental, but it's the last mile from curve to strike selection, and it pairs with the wheel screener and RND.
- **Steal from:** `EazyDuz1t_EzOptions/ezoptions.py find_probability_strikes (L2948), find_delta_strikes (L2990)`
- **Lands in floww:** Helper over the existing prob-distribution output (backend/server.py:496-524) emitting strike_above/strike_below at target probabilities; tiles + cone lines on the implied-move panel.

### #11 · Multi-leg strategy builder: payoff curves, breakevens, PoP, expected P&L, VaR/ES  `[high-impact]`  — value 9 / effort 4
- **Status:** ⬜ not started.
- **Why:** The biggest capability hole: floww has per-position greeks but no way to assemble an iron condor/vertical/calendar and see its payoff diagram, breakevens, max P/L, probability of profit, or tail risk. Merges two candidates — use optionlab (pure-Python pip, selectable PoP engines: BS/array/laplace/montecarlo) as the engine rather than porting harryho71's C++; steal harryho71's MultiLegStrategy.js as the React UI blueprint. Turns the whole GEX/vol read into 'build a trade and see its odds.'
- **Steal from:** `rgaveiva/optionlab run_strategy/Inputs/Outputs + get_pl(); UI from harryho71_option-strategy-pricer/src/frontend/src/components/MultiLegStrategy.js; VaR/ES pattern from RiskMeasures.cpp`
- **Lands in floww:** POST /strategy/evaluate builds optionlab Inputs from the DuckDB chain snapshot (spot, r, mids, per-leg IV floww already computes); returns PoP/expected-P&L/breakevens/payoff arrays; new 'Strategy Builder' tab on Skylit with payoff + greeks-vs-spot curves. Use lognormal σ from the RV/IV service for probability weighting.

### #12 · Whole-book scenario stress-test matrix (spot × IV × time shock grid)  `[high-impact]`  — value 8 / effort 4
- **Status:** ⬜ not started.
- **Why:** Answers 'what happens to my book if spot drops 5% and IV pops 20% over 3 days' — impossible today with only point-in-time portfolio greeks. Pure recompute over the existing position store with floww's own BS pricer; no new data.
- **Steal from:** `George-Dros_Options_Portfolio/functions.py:457-770 (analyze_combined_impact, process_portfolio, compute_portfolio_stats) + 3D surface plots 797-884`
- **Lands in floww:** POST /portfolio/scenario running the shock grid (spot 0.8-1.2x, IV 0.8-1.2x, +7/30/60d decay) over floww's position store; aggregate net greeks + repriced P&L; Plotly surface or spot-vs-time heatmap in Skylit. Shares leg-repricing code with the strategy builder (rank 11).

### #13 · Earnings-volatility screener (IV30/RV30 + term-slope Recommended/Consider/Avoid rule)  `[high-impact]`  — value 7 / effort 4
- **Status:** ⬜ not started. (Blocked on #7 — RV service.)
- **Why:** Floww has zero earnings-event handling. Codifies the well-known sell-rich-pre-earnings-IV strategy with concrete thresholds (ADV ≥ 1.5M, IV30/RV30 ≥ 1.25, slope ≤ −0.00406) and a universe scan by report date. Mostly wiring: RV30 comes from the rank-7 RV service, IV30/term slope from Trinity, earnings dates from yfinance calendar (skip the paid Investing.com scrape).
- **Steal from:** `Acelogic/Earnings-Volatility-Calculator src/calculator.py`
- **Lands in floww:** Earnings-vol screener job pulling yfinance earnings dates, applying the decision rule, emitting candidates into the Flowseeker feed/alerts as event-timed straddle/calendar ideas. Depends on rank 7 (RV service).

### ~~#14 · OCC cleared volume by account type — Customer vs Firm vs Market-Maker panel  `[high-impact]`  — value 8 / effort 5~~
- **Status:** ✅ **DONE** (2026-07-15, this session — occ-volume-scraper).
  - **In:** Wide-schema (10-column row per `(trade_date, ticker)`) `backend/services/occ_scraper.py` mirroring the Finviz/insider pattern: idempotent `init_occ_daily_table(engine)` (`PRIMARY KEY (trade_date, ticker)` + companion `occ_csv_cache` for 24h CSV-text caching), UPSERT `accumulate_today(engine, rows, snapshot_date)`, ASC-order `read_recent_occ(engine, ticker, n_days)`. Pure-logic `compute_occ_summary(ticker, rows)` returns `customer_pct_of_total`, `mm_net_bias` (bullish | bearish | neutral at 15% put/call skew threshold), `mm_call_put_ratio`, `customer_call_put_ratio`, `recent_total_volume`, `n_days_covered`, `warnings`. HTTP layer: `_fetch_occ_csv(report_date)` issues `requests.get('https://marketdata.theocc.com/volume-query', params={"volumeQueryType":"O","accountType":"A","reportType":"D","reportDate":...}, headers={"User-Agent": <Chrome/120>}, timeout=10)` with the 5-business-day rewind fallback mirror of `EazyDuz1t_EzOptions.ezoptions.download_volume_csv (L4010)`. Defensive CSV parser (`csv.DictReader`) accepts multiple header synonyms (Quantity/Volume, Underlying_Symbol/Symbol, Call_Put_Indicator/Cp, Account_Type/Acct) so OCC's column renames don't break ingest. Endpoint `GET /api/occ_volume/{ticker}?days=14&accumulate=true|false` mounted in the canonical steal-three router at :8000. Front-end Tile #5 appended to `frontend/src/components/heatseeker/ConfluenceVelocityRow.jsx` showing `mm_net_bias` chip + `customer_pct_of_total` gauge in slate palette. Pytest suite ships green at `backend/tests/services/test_occ_scraper.py` (zero-state, single-day totals, multi-day uses latest row not sum, mm-bearish-threshold, customer-pct ratio, valid-CSV, missing-quantity, extra-columns ignored, missing-account-type-filter, trailing whitespace, ticker-isolation in CSV, empty CSV, quantity coercion incl. comma/quote/clamp-<0, init-idempotent, accumulate UPSERT, multi-ticker accumulation, read-N limits, ticker-isolation in DuckDB, accumulate-empty no-op, weekend-fallback rewinds Sun/Sat to Fri, params canonical keys, FALLBACK_DAYS=5, REQUEST_TIMEOUT_S≥5, BASE_URL canonical).
- **Why:** The only genuinely NEW free data source in the batch: answers 'WHO traded' (dealers vs customers, call/put split, dealer directional bias), which yfinance aggregate volume/OI can never reveal. Tempered by T+1 daily granularity and an unofficial endpoint — port EzOptions' 5-day date-fallback and defensive CSV parsing wholesale.
- **Steal from:** `EazyDuz1t_EzOptions/ezoptions.py: download_volume_csv (L4010), get_params_for_date (L3793), process_market_maker_data (L3819), charts L3939/L6540-6620`
- **Lands in floww:** New backend service (httpx GET https://marketdata.theocc.com/volume-query, browser User-Agent, no key) -> /api/occ-volume/{ticker} cached daily in DuckDB -> React 'Who Traded' panel next to Flowseeker with customer-vs-dealer call/put ratios.

### #15 · Historical options-strategy backtester over accumulated chain snapshots (optopsy)  `[ambitious]`  — value 6 / effort 4
- **Status:** ⬜ not started.
- **Why:** Turns floww's cached daily chains into a strategy lab — 'how would selling 30Δ SPY put spreads have done since I started caching.' Strategic long-term capability, but value ramps with snapshot depth: near-term it only covers the window already stored, so start the adapter now and let history accrue (optionally seed from free CBOE EOD).
- **Steal from:** `michaelchu/optopsy (pandas-native strategy constructors: verticals, straddles/strangles, condors, calendars)`
- **Lands in floww:** backend/backtest/optopsy_adapter.py mapping floww's cache/ + DuckDB chain snapshots into optopsy's column schema; Backtest tab in React; feed results into existing sizing/journal modules (floww_trades_v2 via tradeMath.js conventions).

### #16 · Arbitrage-free SVI vol surface + no-arb violation feed  `[ambitious]`  — value 8 / effort 6
- **Status:** ⬜ not started. (Sequenced last by design — ship after the #5 solver proves the clean-mid pipeline; #5 is now in.)
- **Why:** The capstone data-quality layer: a Gatheral 5-param SVI fit per expiry gives a smooth surface valid at ANY strike/expiry (needed to price off-grid legs for the strategy builder) and de-noises inputs to GEX/flip/King-node/RND; butterfly/parity/calendar violations double as a rich/cheap mispricing feed. Last because effort is highest and calibration on thin retail chains is the real risk — ship after the rank-5 IV solver proves the clean-mid pipeline.
- **Steal from:** `XanderRobbins/Arbitrage-Free-Volatility-Surface vol_surface/{svi.py, arbitrage.py, iv_solver.py, surface.py} (skip heston.py initially)`
- **Lands in floww:** Port to backend/analytics/vol_surface_svi.py; fit on cached chain snapshots; persist 5 SVI params per expiry in DuckDB via execute_write; serve interpolated IV to GEX/greeks/RND/strategy evaluator; arbitrage-violation badges on the Trinity smile view and a 'no-arb breaks' feed in Flowseeker.

## Roadmap — signal & sentiment layer (from run-1, merged back in)

_These 6 came from mining the flow-scanner + ML/sentiment repos; they cover a dimension the vol/GEX items above don't touch._

### ~~Multi-model sentiment reconciliation engine (VADER compound + TextBlob polarity with cross-source agreement gate)  — value 4 / effort 1~~
- **Status:** ✅ **DONE (partial)** (2026-07-11, sentiment-session — algorithm + endpoint in, feature-column wiring deferred).
  - **In:** Pure-logic `score_text(text) → (polarity, subjectivity, label)` with the shirosaidev/stocksight AND-gate (BOTH VADER compound ≥ ±0.05 AND TextBlob polarity sign must agree for positive/negative label, else neutral), `clean_text` + `clean_text_sentiment` helpers ported verbatim, `aggregate_sentiment(texts) → TickerSentiment-shaped dict` (single-dispatch per text — VADER and TextBlob each called once), defensive degradation via `VADER_AVAILABLE`/`TEXTBLOB_AVAILABLE` flags + `unavailable` sentinel label, via `backend/services/sentiment.py`. ~16 hand-verified unit cases ship green. Endpoint `GET /api/sentiment?text=...` mounted in the canonical steal-three router at :8000.
  - **Deferred:** (a) the upstream `social_flow_pipeline.py::TickerSentiment` class still has broken decorators (no `@dataclass` on the class) and no code populates it — the 1-line decorator fix + wiring `aggregate_sentiment()` into the existing `/api/social/sentiment/{ticker}` cached route is a focused followup. (b) feature-column wiring into `services/composite_flow_score.py` + `services/ml_realtime_features.py` is intentionally decoupled from this ship.
- **Why:** The actual sentiment SCORING that floww is missing. floww's social_flow_pipeline.py declares a TickerSentiment dataclass with avg_vader and avg_textblob fields (L149-150) but ships NO code that computes them — there is no SentimentIntensityAnalyzer, no TextBlob import, and the dataclass decorators are even broken. This is the concrete implementation: score text with both VADER (compound) and TextBlob (polarity/subjectivity), and only label positive/negative when BOTH agree (and optionally a third web source), else neutral. The agreement gate materially cuts false-positive sentiment flags on noisy finance text.
- **Steal from:** `shirosaidev_stocksight` — shirosaidev_stocksight/sentiment.py — sentiment_analysis() (L467-528), agreement logic at L493-506; clean_text()/clean_text_sentiment() (L412-428)
- **Lands in floww:** Drop-in function services/sentiment.py::score_text(text) returning (polarity, subjectivity, label) using vaderSentiment + textblob (add both to requirements). Feed it headlines from the news feed (finding 1) or tweets; aggregate per-ticker into the existing TickerSentiment model and populate the /api/social/sentiment/{ticker} endpoint that currently returns an empty stub. Add sentiment as a feature column for ml_realtime_features / composite_flow_score.

### ~~Opportunity Engine: regime classification + opportunity scoring + risk-defined trade-idea construction  — value 4 / effort 2~~
- **Status:** ✅ **DONE** (2026-07-15, this session — opportunity-engine).
  - **In:** Pure-logic `backend/services/regime_opportunity.py` shipping the 6-cell (BULL/RANGING/BEAR × QUIET/MILD/ACTIVE/STRESSED) regime grid, opportunity_score = `10·|trend| + 1.5·momentum + 0.5·alignment − 2·vol_penalty + 1·mean_rev_bonus` clamped [0, 10], and a deterministic trade-idea arbitration (gamma-sign × IV-rank → debit_spread / credit_spread / iron_condor / iron_fly / wheel_csp / bear_put_debit / defensive_hedge / no_trade). `GET /api/opportunity/{ticker}` mounted in `backend/routes/steal_three.py` (canonical :8000). 16-case unit suite ships green at `backend/tests/services/test_regime_opportunity.py` (6-cell full-table walk, score clamping [0..10] + [2.5..5.0] WATCH band, vol_penalty table, mean-rev sweet-spot, arbitration panics, malformed-input graceful, NaN-safe scoring).
- **Why:** A per-ticker DIRECTIONAL trade-idea layer that floww lacks. floww's Flowseeker ranks FLOW (whale/lotto/hedge) but never emits a ranked, risk-defined trade recommendation with a regime label and invalidation. This engine maps (trend x realized-vol) into a 6-cell regime grid (Trending Low-Vol / High-Vol / Range Bound / Choppy / Downtrend / Panic), computes opportunity_score = |trend| + momentum_bonus + alignment_bonus - vol_penalty + mean_rev_bonus clamped 0-10, then arbitrates with gamma sign (positive gamma -> mean-reversion bias, negative gamma -> directional bias) and IV rank (elevated IV -> prefer premium selling) to output {regime, opportunity_tier, direction, trade_type, trade_bias, invalidation}. Every input (trend/momentum/extension scores, realized_vol_20d, gamma flip sign, IV rank) is data floww ALREADY computes, so this is a pure synthesis/ranking layer on top of existing signals.
- **Steal from:** `jwolberg_options-scanner` — docs/opportunity_engine.md (STEP 1 regime table, STEP 2 scoring formula, STEP 4 signal arbitration) + docs/api_v2_ticker_output_spec.md (trade_recommendation + market_state output shape). NOTE: the paid Trading Volatility upstream computes it; only the spec/formula is in-repo, so port the DESIGN, not code.
- **Lands in floww:** New FastAPI endpoint /opportunities that pulls each universe ticker's existing floww signals (GEX gamma-flip sign already in the gamma module; IV rank/skew from Trinity; realized vol from price history; trend/momentum from a simple MA+ROC on yfinance OHLC) and returns a ranked top-3-5 list. Surface as a new Skylit 'Trade Ideas' card next to Flowseeker. Reuses tradeMath.js for the risk plan (entry/stop from expected-move sigma wings). Data source: 100% floww-internal, no new feeds.

### Free financial-news headline ingestion (Yahoo Finance scraper + follow-links to article body text)  — value 4 / effort 2
- **Status:** ✅ **DONE** (2026-07-15, this session — news-feed).
  - **In:** Pure-Python `backend/services/news_feed.py` mirroring the `insider_scraper.py` template (urllib + bs4 only, zero paid APIs): `fetch_ticker_news(ticker, limit=15)` returns `[{ticker, title, url, publisher, scraped_at}]` from `https://finance.yahoo.com/quote/{ticker}/news` with deterministic UA rotation across 3 fingerprints and defensive fallback to `/quote/{ticker}` if `/news` 404s. `fetch_article_text(url, max_paragraphs=10)` follows a link and extracts paragraphs — Yahoo's native `<div class="caas-body">` for in-domain articles, generic `<p>` walk with 40-char length floor for external links (filters UI chrome). 14-case unit suite ships green at `backend/tests/services/test_news_feed.py` (yahoo-native layout, dedupe-by-url, limit-respected, ticker-uppercased, HTTP 429/403 graceful-degrade, empty/malformed HTML, h2+h3 dual capture, yahoo-native caas-body, generic-fallback, paywall-warning, HTTP-404, orphan-h3 skip, length-floor). Endpoints `GET /api/news/{ticker}?limit=15` + `GET /api/news/article?url=...&max_paragraphs=10` mounted in the canonical steal-three router at :8000 (static `/api/news/article` declared BEFORE the `{ticker}` catch-all per the insider-scraper route-ordering convention).
  - **Followup ✅ DONE** (2026-07-15, this session — news-feed persistence): DuckDB persistence (`news_daily` + `news_html_cache`, 24h LRU HTML cache evicting at 5000 rows) — `accumulate_today` UPSERT (PRIMARY KEY (snapshot_date, ticker, url)) + `read_recent_news` ASC read + `compute_news_summary` pure-logic analytics (n_headlines, n_unique_domains, top-3 publishers, has_catalyst_news flag with earnings/fda/merger/spinoff/upgrade/downgrade/buyout/phase-2/phase-3/approval/guidance/restructuring/layoff keyword set). Route updates: `GET /api/news/{ticker}?accumulate=true` writes today's snapshot before responding (standard cron pattern); new `GET /api/news/{ticker}/history?days=14&accumulate=false` returns the persisted history + summary. Scheduler: `backend/services/scheduler.py::_poll_news_for_universe` runs daily (every 1440 ticks at 60s interval = 24h) over the top-10 universe (SPY/QQQ/AAPL/TSLA/NVDA/AMZN/MSFT/META/GOOGL/AMD) via `loop.run_in_executor` for non-blocking network I/O. ~12 new test cases in `backend/tests/services/test_news_feed.py` (init-tables-and-indexes, accumulate-upsert-proper-params, accumulate-drops-missing-keys, accumulate-empty-no-op, accumulate-idempotent-same-day, read-recent-passes-correct-sql, read-recent-returns-rows-asc, read-recent-db-exception-yields-empty, cache-within-TTL-returns-cached, cache-stale-refetches-and-updates, cache-read-exception-falls-through, compute-summary-empty, compute-summary-groups-by-publisher-top-3, compute-summary-catalyst-detected, compute-summary-no-catalyst, compute-summary-skips-non-dict).
- **Status:** ⬜ not started.
- **Why:** A zero-cost financial NEWS feed per ticker. floww currently has NO market-news source at all — the only scraper in the backend (services/research/discovery.py) pulls arXiv/HuggingFace/GitHub/SSRN research papers, not headlines. This adds a NewsHeadlineListener-style poller that scrapes Yahoo Finance quote pages for <h3> headlines, dedupes, optionally follows /news/ links, and extracts up to N paragraphs of article body — all via requests+BeautifulSoup, no paid API. It gives floww a genuine catalyst/news stream to pair with GEX and flow.
- **Steal from:** `shirosaidev_stocksight` — shirosaidev_stocksight/sentiment.py — NewsHeadlineListener class (L265-337), get_news_headlines() (L339-382), get_page_text() (L385-409)
- **Lands in floww:** New backend service (e.g. services/news_feed.py) modeled on NewsHeadlineListener.get_news_headlines()/get_page_text(); source = https://finance.yahoo.com/quote/{ticker} via existing HTTP client. Wire into scheduler.py as a periodic poll, persist headlines to DuckDB, and surface under the already-stubbed /api/social route (routes/social_flow.py) or a new /api/news route. Replaces the broken xurl/paid-X dependency in social_flow_pipeline.py with a free source.

### Signal-to-realized-move event-study engine (timestamp an event, sample price at fixed forward horizons, log the move)  — value 4 / effort 3
- **Status:** ⬜ not started.
- **Why:** A generic 'did this signal actually move the stock?' measurement loop. floww's realized-outcome tracking (services/ml/outcomes.py) is hard-wired to its OWN ML predictions and only computes a single next-day open->close label. jasti's perform_financial_analysis records the price at event time and then re-samples it at fixed intervals (T+0, T+30s, ... x8), writing the forward path for each event. Generalized, this lets floww attach an empirical forward-return distribution to ANY event type — a sentiment spike, a news headline, a WHALE/sweep flow signal, a gamma-flip cross — building a per-signal edge/backtest that floww's prediction-only tracker cannot express.
- **Steal from:** `jasti_Stock-Predictor` — jasti_Stock-Predictor/src/main.py — Predictor.perform_financial_analysis() (L74-94) and the event dispatch in checkIfValidTweet()/stream loop (L98-161)
- **Lands in floww:** New services/event_study.py that, on any emitted alert/signal, snapshots yfinance/cvforge spot and schedules follow-up samples at configurable horizons (e.g. +5m/+30m/+1d), persisting {event_id, signal_type, t, price} rows to DuckDB via execute_write. Aggregate into hit-rate/avg-forward-return per signal_type and expose alongside the Flowseeker/alerts UI. Reuse the existing scheduler.py for the delayed sampling instead of jasti's raw threads/sleep.

### ~~OI-weighted expected/consensus price per expiration (chain-implied pin)  — value 3 / effort 1~~
- **Status:** ✅ **DONE** (algorithm + endpoint 2026-07-11, chain-consensus session; daily drift tracking completed 2026-07-15, consensus-pricing-daily session).
  - **In (2026-07-11):** per-expiry OI-weighted `(strike + premium)` and `(strike - premium)` consensus price + supporting total/call/put OI + `avg_call_premium` / `avg_put_premium` (OI-weighted premiums per side) + an `overall` top-line blend via `backend/services/consensus_pricing.py`. Endpoint `GET /api/chain_consensus/{ticker}?expiries=N` mounted in the canonical steal-three router at :8000 (re-uses the existing `_load_multi_expiry_chain` helper). 16 hand-verified unit cases ship green (premium resolver paths, OI variants, ties, asymmetry, sentinel, type-aliases).
  - **In (2026-07-15):** Pure-logic `compute_consensus_drift(snapshots)` in `backend/services/consensus_pricing_daily.py` — returns today_consensus, today_call_premium, today_put_premium, today_spot, yesterday_consensus, drift_consensus_1d, drift_consensus_Nd, drift_skew_today (=abs(today_call_premium - today_put_premium)), drift_skew_change_1d, convergence_score (`clamp(1 - |today_consensus - today_spot| / (0.05 * today_spot), 0, 1)` matches the max_pain pin_probability_score convention), direction_label (`call_side_pulling_up` when today_consensus > today_spot + 0.005*spot AND avg_call_premium > avg_put_premium, `put_side_pulling_down` symmetric, `balanced` when delta large but premiums tied, `stable` within ±0.5% spot band, `insufficient_data` otherwise), n_days_covered, warnings. DuckDB I/O helpers: `init_consensus_daily_table(engine)` (CREATE TABLE IF NOT EXISTS + PRIMARY KEY (snapshot_date, symbol, expiry) where expiry="" is the OVERALL row vs ISO string for per-expiry + 3 indexes), `accumulate_today(engine, ticker, contracts, snapshot_date=None)` (calls `compute_consensus_per_expiry` + `compute_overall_consensus` internally; one row per expiry + one row for overall blend; ON CONFLICT idempotent UPSERT — same-day re-runs overwrite rows cleanly), `read_recent_drift(engine, ticker, n_days, expiry=None)` (DESC LIMIT then reverse to ASC for natural drift walks; optional expiry filter returns per-expiry row + overall row baseline). Reuses defensive helpers `_safe_float` / `_to_date` / `_coerce_to_date` from `backend/services/max_pain_drift.py` (imported, not duplicated — private helper boundary is honored because callers go through the public functions). Endpoint `GET /api/consensus_drift/{ticker}?days=14&expiry=YYYY-MM-DD&accumulate=true|false` mounted in canonical steal-three router at :8000 (uses `services.consensus_pricing_daily` + `server.fetch_spot_and_chains` + `services.duckdb_engine.db`). 22-case unit suite ships green (18 pre-existing + 4 new for PARTIAL → DONE: per-expiry-writes-N-rows-and-UPSERT-updates/schema-migration-drops-legacy-then-recreates/scheduler-has-poll-method/route-default-days-30) at `backend/tests/services/test_consensus_pricing_daily.py` (empty/single/2-snap/7-day stable/call-pulling-up/put-pulling-down/high-convergence/reverse-chron/NaN/missing-field/future-dated/documented-keys/skew-tilt-detection/balanced-premium + init-idempotent/accumulate-UPSERT/three-ticker-isolated/read-recent-N/expiry-filter-baseline/empty-table/DB-exception-suppressed). Frontend: `ConsensusDriftTile` in `ConfluenceVelocityRow.jsx`.
  - **Deferred:** the dotted-line `chain consensus` overlay on the strike x expiry Heatseeker matrix in the Skylit layout.
- **Why:** A new per-expiry scalar floww does not compute: the open-interest-weighted 'expected price' the whole chain is positioned around. For calls it weights (strike+premium)*OI, for puts (strike-premium)*OI, then blends: option_expect_price = [Sum(call_expect*call_OI) + Sum(put_expect*put_OI)] / Sum(all_OI). This gives a market-consensus/breakeven-weighted price magnet per expiry that is DISTINCT from floww's GEX King node (which is a dealer-gamma concentration strike, not an OI-premium-weighted breakeven). Tracking it day-over-day shows where positioning consensus is drifting.
- **Steal from:** `czong_option_chain_unusual_activity_detect` — main-google.py lines 13-33 and main-yahoo.py lines 22-53 (call_expect_price = strike+premium, put_expect_price = strike-premium, OI-weighted blend grouped by expire_date)
- **Lands in floww:** Add a pure function to floww's options-math module (alongside greeks/tradeMath) that takes the chain floww already fetches per expiry and returns expect_price per expiry + a blended term curve. Overlay a dotted 'chain consensus' line on the existing strike x expiry Heatseeker matrix, and store daily values so the term-structure view can show consensus drift. Data source: existing yfinance/cvforge option chain (strike, last/mid premium, OI) - no new fetch.

### ~~Finviz insider-trading scraper (latest / top-by-value / per-ticker)  — value 3 / effort 2~~
- **Status:** ✅ **DONE** (2026-07-15 — insider-scraper; status line added during the 2026-07-15 restore, verified against bytes + green suite).
  - **In:** `backend/services/insider_scraper.py` (Finviz fetch via requests+BeautifulSoup with header-based table lookup instead of the upstream hardcoded table[5]/table[9], DuckDB cache, defensive parser, zero-state summary). Endpoints `GET /api/insider/top`, `GET /api/insider/latest`, `GET /api/insider/{ticker}` mounted in the canonical steal-three router at :8000. Suite green at `backend/tests/services/test_insider_scraper.py`. Frontend: `InsiderPressureTile` in `ConfluenceVelocityRow.jsx` (net buy pressure, buy/sell counts, CEO-buy chip, whitelist-of-empty warning classification).
  - **Polish ✅ (2026-07-16, this session):** RFC-7231 split — `?accumulate=true` query param on the 3 GET handlers is now deprecated + ignored (deprecation warning surfaced in `warnings` array). Writes moved to 3 dedicated POST endpoints: `POST /api/insider/top/accumulate`, `POST /api/insider/latest/accumulate`, `POST /api/insider/{ticker}/accumulate`. Each POST returns `{..., written_n_rows}` so cron jobs can verify the write succeeded. Backward compatible — callers passing `?accumulate=true` on GET don't break, they just emit the deprecation warning. Tests at `backend/tests/routes/test_insider_routes.py` (TestClient + monkeypatch spy on `services.insider_scraper.accumulate_today`) verify GET never calls `accumulate_today` while POST does, and the static-route resolution always beats the `{ticker}` catch-all. **Followup sweep:** the same RFC-7231 violation lives on `GET /api/max_pain_drift/{ticker}?accumulate=true`, `GET /api/consensus_drift/{ticker}?accumulate=true`, `GET /api/news/{ticker}/history?accumulate=true`, `GET /api/occ_volume/{ticker}?accumulate=true` — generalize the polish in a focused follow-up commit.
- **Why:** A corporate insider-transaction signal floww has zero coverage of. Three scrapers pull Finviz's insider-trading tables into DataFrames: getLatestInsiderTrading(), getTopInsiderTrading() (filtered to transactions >$100k, last 7 days, sorted by transaction value), and getInsiderTradingFor Ticker(t). Cross-referencing a Flowseeker unusual-flow alert against a same-week insider BUY on the same ticker is a genuinely new corroboration lens (smart-money options flow + insider equity buying) that no current floww module provides.
- **Steal from:** `Buzzfund_UnusualOptions` — insider.py: getLatestInsiderTrading() (lines 12-28), getTopInsiderTrading() (lines 30-46, note the ?tv=100000&tc=7 filter), getInsiderTradingForTicker() (lines 57-64)
- **Lands in floww:** Wrap the three functions as a small fetch module; expose GET /insider/{ticker} and a daily 'top insider buys' pull. In the Flowseeker scanner, add an 'insider' badge/column that lights up when a scanned ticker also appears in the recent Finviz insider-buy table. Data source: Finviz public HTML (free) via requests+BeautifulSoup; needs a cache + polite rate-limit and a parser-fragility guard (Finviz table index is hardcoded to table[5]/table[9], so add a header-based lookup). Best treated as a soft/optional signal given scraper brittleness.

## Frontend surface (status as of 2026-07-15)

- **Heatseeker Row 3 ("Confluence & Velocity")** now mounts, in temporal order: the steal-list top-3 (`DualGEXBadge` #1, `IVMidBadge` #5, `WheelIncomeScreenerPanel` #3), the historical-signals band (`ConfluenceVelocityRow` — max-pain drift #9, consensus drift, insider pressure, strike cone #10, OCC who-traded #14), then the pre-existing `VelocityModeBadge` + `TrinityConfluenceMeter`. Pinned by `HeatseekerDashboard.test.jsx` (15 → 17 child panels as of 2026-07-15 Row 4 mount).
- **Heatseeker Row 4 ("Expected Moves / Trade Ideas")** now mounts `StrikeConeBadge` (#10 — SVG implied-move cone: outer 1σ band + inner ½σ band + amber dashed spot marker + 16Δ/30Δ wing sub-tiles) and `OpportunityBadge` (#8 — regime pill + trade_type pill + opportunity_score gauge ≥ tier traffic-light + trade_bias micro-pill + invalidation rule). Eagerly rendered (no `LazyRow`) so first paint shows them. The prior "Advanced Structure" Row 4 shifts to Row 5; the prior "Node Classification" Row 5 shifts to Row 6; the prior "Greek Exposures" Row 6 shifts to Row 7. Pinned by the bumped `HeatseekerDashboard.test.jsx` child-panel count (15 → 17).
- **SkylitDashboard** mirrors the top-3 band (`skylit-steal-list-band` testid) so the signals appear on both the default Heatseeker page and the Sidebar→Skylit layout. Row 4 badges are Heatseeker-only per the user-mandated scope (no sketch to add them to the Skylit bottom band this turn).
- **`StealThreePreview.jsx`** (all three top-3 on one page with ticker quick-picks) exists, and navConfig ships a "Steal Three" Decoder tab — **but App.js has no `page === "steal-three"` branch, so the tab renders nothing.** App.js is architect-frozen: the 3-line mount needs Nav's explicit approval.

## New repos worth cloning (not yet in data/github-repos/cloned)

- **rgaveiga/optionlab** — mature pip lib — multi-leg PoP (BS/array/MonteCarlo), payoff, expected P&L → powers the Strategy Builder (#11)
- **American-Dynasty/GEX-Dashboard** — React + FastAPI (floww's exact stack) — GEX engine + key levels (gamma flip / call+put walls / max pain), liftable components
- **aaguiar10/gflows** — spot-shifted exposure profiles + 0DTE volume-for-OI (#2, #6)
- **iAmGiG/gex-llm-patterns** — dual-GEX (OI vs volume) + regime-persistence classifier (#1, #8) — already cloned
- **jasonstrimpel/volatility-trading** — realized-vol estimator suite + vol cones (#7)
- **michaelchu/optopsy** — pandas-native options backtester (#15)
- **XanderRobbins/Arbitrage-Free-Volatility-Surface** — Gatheral SVI arb-free surface (#16)
