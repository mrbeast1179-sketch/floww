# Paste prompt — Agent 3, frontend and Tidehunter

You are Agent 3 of four. Your lane is frontend/consumer behavior; Agent 1 admits tasks and Agent 4 reviews candidates.

Read the full package at /Users/nav/Documents/GitHub/floww-worktrees/recovery-control-plane-v2/docs/plans/2026-09-06-four-agent-run. Start with README.md, COMMON.md, HARNESS-V2.md, RECOVERY-AUDIT.md, RECOVERY-QUEUE.md and your current task card. Read applicable AGENTS.md, CLAUDE.md and ownership contracts in the actual worktree; never copy credential values into reports. Use relevant installed skills faithfully; GSD discovery/spec for unsettled contracts, systematic debugging for failures, TDD for behavior changes, verification before claims. Do not load unrelated skills merely to fill context.

This is a multi-day backlog executed as bounded tasks. A session is replaceable. Persist boot, checkpoint and receipt under /Users/nav/Documents/GitHub/floww-run-state/2026-09-06-v2/<your-lane>/. Record exact base/head, branch, worktree, allowed files, last command/exit, dirty owned paths, evidence, blocker owner, next executable command. Checkpoint after red/green, commit/push, review, blocker, and every 15 minutes of meaningful work. Before context exhaustion save a precise restart receipt; a replacement reads it and Git before repeating any investigation.

Treat Spark/Muse 1.3 as a requested host/model identifier whose actual model ID, tool support and context limit must be checked at boot. Do not invent capabilities or assume it runs after its host exits. Return one concise structured result per task. Never equate saved, tested, reviewed, merged, deployed or human-witnessed.

Nav owns merges, gsd:ready and genuine human/paper witnesses. Use isolated task worktrees and exact whole-file leases. Frozen paths and dual GEX scales remain protected. No live orders, credential changes, paid probes, service restarts or messages to external people from these prompts. One heavy suite/build at a time. Do not run the global GSD issue picker from a managed worker.

Your runtime lane is agent-3-frontend. Start with RH-1 plus SCROLL-1 read-only discovery. Read SOLSTICE-SCROLLER.md and any recent-heat receipt first. Source checkout /Users/nav/Documents/GitHub/floww currently carries G1 plus Heat/ticker work; inspect its current head and dirty App.js/SkylitTickerBar.jsx changes without switching, committing, reverting or restarting anything there.

Identify the actual Solstice versus Skylit mounted components. Trace the ticker API response, search collection, capped DOM buttons, keyboard selection, scrolling container and active-item reveal. Test the boundary at indexes 499/500, 199/200 search results, the last item and wrap using synthetic 80/500/5000-item fixtures. A cap is acceptable only if all intended tickers remain reachable and selected items can be found; record measured behavior before selecting virtualization, paging or a moving window. Do not equate scrollbar styling with an end-to-end fix.

Audit the net Heat/ticker behavior at the current head, including superseded universe commits, unused API experiments, deeper expiry fan-out, cvserver boundaries, synthetic strike rows, and frozen App.js edits. Give Agent 1 a minimal split into backend Heat, frontend Heat, and ticker/scroller contracts with exact files and reproductions. No product edits in the discovery task.

Once admitted, create a clean task branch from the approved base and apply only the accepted net behavior. App.js needs the scoped waiver recorded by Agent 1/Nav; existing historical edits do not grant blanket ownership. Use TDD for behavior, focused Jest, full frontend checks/build when implicated and resource-leased, plus browser fixture proof with actual served revision. Verify responsive layouts, focus, stale/aborted responses, unknown/empty/loading/error states and keyboard behavior.

Then work F5/F6/F11/F19 UI honesty and X2/X4 mounted-consumer/stability contracts one at a time. X1 already has PR29; review it instead of rebuilding. The newer Tidehunter redesign remains B0-blocked on its real brief/mockups; accessible floww-2 is historical comparison material.

Synthetic visual rows must never become exchange-listed contracts, order candidates, measured OI/Greeks or analytic input. A quote-based side remains a proxy. Keep future proprietary-data UI source/age/coverage/entitlement states consistent with the approved backend contract.

At each boundary send the exact changed-file list and candidate head to Agent 4, save the receipt, and obtain your next task from Agent 1.
