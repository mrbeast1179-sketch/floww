diff --git a/frontend/src/App.js b/frontend/src/App.js
index c669e3a..2dfe4ba 100644
--- a/frontend/src/App.js
+++ b/frontend/src/App.js
@@ -189 +189 @@ function ApHeader({ page, ticker, onTickerChange, tickers, data, onSignOut, user
-              tickers={[...(tickers.trinity || []), ...(tickers.default || []), ...(tickers.popular || [])]}
+              tickers={[...(tickers?.trinity || []), ...(tickers?.default || []), ...(tickers?.popular || [])].slice(0, 200)}
diff --git a/frontend/src/components/heatseeker/SkylitTickerBar.jsx b/frontend/src/components/heatseeker/SkylitTickerBar.jsx
index 74f3498..42e0c61 100644
--- a/frontend/src/components/heatseeker/SkylitTickerBar.jsx
+++ b/frontend/src/components/heatseeker/SkylitTickerBar.jsx
@@ -28,2 +28,4 @@ function SkylitTickerBar({
-  // Combine every ticker set the API returns into one scrollable list.
-  // trinity (^SPX, SPY, QQQ) + default (17) + popular (75) = 80 unique.
+  // trinity (^SPX, SPY, QQQ) + default (17) + popular (up to 5000 from
+  // /api/tickers/all) = every tradable name. Cap rendered buttons at 500 for
+  // smooth scrolling; search box is the fast path for deep cuts.
+  const RENDER_CAP = 500;
@@ -46,0 +49,2 @@ function SkylitTickerBar({
+    // Slice for render perf — the full list is still available via search.
+    if (out.length > RENDER_CAP) return out.slice(0, RENDER_CAP);
@@ -50 +54,9 @@ function SkylitTickerBar({
-  const totalCount = tickerList.length || 0;
+  const totalCount = (() => {
+    const seen = new Set();
+    for (const arr of [tickers?.trinity, tickers?.default, tickers?.popular]) {
+      if (!arr) continue;
+      for (const t of arr) { const k = t.toUpperCase(); seen.add(k); }
+    }
+    if (seen.size === 0) { for (const t of DEFAULT_TICKERS) seen.add(t); }
+    return seen.size;
+  })();
